# T16 build check (2026-09-09)

- Working copy: `interpolation_paper_rans_results_closure` (git; baseline commit 9e2d6cd = byte copy of the delivered package). The package zip and its SHA-256 quoted in the plan were not available locally; the delivered folder was hashed instead (`closure/csv_sha256_baseline.txt`, 68 CSVs; the two historical manifests match 39/39 and 53/53).
- `make` with the distribution TeX (TeX Live 2019, pgfplots 1.16) **fails**: `Package pgfplots Error: 'compat=1.18' is unknown` (`closure/build_baseline.log`, first attempt). pgfplots 1.18.3 from CTAN was installed under `~/texmf/tex/latex/pgfplots`; with it the delivered `paper.tex` builds in 42 s (pdfTeX + biber), no undefined references or citations. This is a declared-dependency issue, not a source defect; the README should name pgfplots ≥ 1.18.
- `scripts/build_plot_data.py` regenerates four `generated/*_orders.csv` with last-digit differences (≤ 1.3e-15, numpy 1.24 formatting); numerically identical, not byte-identical. The preservation check should compare numbers at a tolerance or the views should be written with fixed precision.
- `scripts/build_rans_data.py` with the corrected RANS exports (`rans_oneshot_lags.csv`, `rans_pressure_probe.csv`: predicted lag now 2.88·d; `rans_drift_trends.csv`: AR(1) columns) and the current report runs to completion (scratch test); only `generated/rans_oneshot_summary.csv` and `rans_pressure_probe_audit.csv` change. The manuscript table column labelled `1000d/c` (l. 1557) must be relabelled and its precision changed before those exports are copied in.
- `paper.bbl` and `paper.pdf` are regenerated by the build (different bytes, same content).

## T06 result (2026-09-10): initial-residual identity from double-precision probes

Source: `data/naca_initial_residual_identity_double.csv` (SteadyNACA/BDF2/bdf2_identity_double.py on the
RD_* probes, PARAVIEW_ASCII with 17 digits, current code revision); summary in
`data/naca_initial_residual_identity.csv`. Relative identity error ||r - p|| / ||p||, all fields,
latest-full pair (older-native identical):

| method | Euclidean norm | M^-1 norm | cosine | max componentwise |
|---|---|---|---|---|
| NN1 | 1.7e-7 | 1.7e-7 | 1.0000000000 | 1.6e-7 |
| NN2 | 1.3e-7 | 1.3e-7 | 1.0000000000 | 5.6e-8 |
| BAR2 | 1.4e-7 | 1.3e-7 | 1.0000000000 | 5.8e-8 |
| GP2 | 4.5e-6 | 1.8e-3 | 1.0000000000 | 9.9e-6 |
| GP3 | 1.3e-6 | 6.1e-4 | 1.0000000000 | 3.4e-6 |

The point-value methods satisfy the identity to 1e-7 in both norms. The Galerkin transfers
carry a 1e-6 Euclidean / 1e-3 M^-1-norm residual concentrated in Momentum_y (GP2 8.8e-6
Euclidean, 5.8e-3 M^-1): the manuscript's identity statement must quote the norm and the
method family, not a single number. The single-precision (float32 VTU) export gave 2e-8 for
every family, i.e. it hid the Galerkin discrepancy; only the double-precision probes are
evidence.

Pending (set up 2026-09-10, not run): `SteadyNACA/BDF2/bdf2_identity_localize.py` locates the
mismatch by region (wall / smallest volumes / rest), lists the worst nodes, and tests candidate 1
by rescaling the predicted forcing with the node-wise r/p ratio of NN1. No ranks needed.
